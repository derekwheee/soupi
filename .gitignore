node_modules/
dist/
coverage/

# Env files — keep the tracked templates, ignore real ones (.env, .env.dev, .env.prod, …)
.env*
!.env.example
!.env.deploy.example

*.log
.DS_Store

# Editor — keep shared project settings, ignore everything else
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# Local scratch
stash/

# Python
python/venv/
python/__pycache__/
python/*.pyc
